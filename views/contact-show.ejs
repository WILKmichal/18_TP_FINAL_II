<%- include('layout-start.ejs') -%>
<div class="back">
  <a class="" href="/">
    <i class="fa-solid fa-arrow-left-long"></i> 
    <small>Retour à la liste</small>
  </a>
</div>
<% if(contact){ %>
<h2>Contact</h2>
<div class="card">
  <div class="card-body">

    <% if(contact.firstName) {%>
    <p>Prénom:<%= contact.firstName %></p>
    <%}%>
<% if(contact.lastName) {%>
    <p>Nom:<%= contact.lastName %></p>
    <%}%>
<% if(contact.organization) {%>
    <p>Organisation:<%= contact.organization %></p>
    <%}%>
<% if(contact.photo) {%>
    <p>Photo:<%= contact.photo %></p>
    <%}%>
<% if(contact.workPhone) {%>
    <p>Téléphone:<%= contact.workPhone %></p>
    <%}%>
<% if(contact.birthday) {%>
    <p>Anniversaire:<%= contact.birthday %></p>
    <%}%>
<% if(contact.title) {%>
    <p>Titre:<%= contact.title %></p>
    <%}%>
<% if(contact.url) {%>
    <p>URL:<%= contact.url %></p>
    <%}%>
    <div class="d-flex justify-content-between">

      <a href="<%= "/contact/update/" + contact.id %>" class="btn btn-primary">
        Modifier
      </a>

      <form action="/downloadExcel" method="GET">
        <input type="hidden" name="firstName" value="<%= contact.firstName %>"/>
        <input type="hidden" name="lastName" value="<%= contact.lastName %>"/>
        <input type="hidden" name="organization" value="<%= contact.organization %>"/>
        <input type="hidden" name="photo" value="<%= contact.photo %>"/>
        <input type="hidden" name="workPhone" value="<%= contact.workPhone %>"/>
        <input type="hidden" name="birthday" value="<%= contact.birthday %>"/>
        <input type="hidden" name="title" value="<%= contact.title %>"/>
        <input type="hidden" name="url" value="<%= contact.url %>"/>
        <button type="submit" class="btn btn-danger">exportExcel</button>
      </form>


      <form action="/contact/delete" method="GET">
        <input type="hidden" name="id" value="<%= contact.id %>"/>
        <button type="submit" class="btn btn-danger">Supprimer</button>
      </form>
    </div>
    <% } else { %>
    Aucun contact trouvé
    <% }%>

  </div>
</div>

<%- include('layout-end.ejs') -%>